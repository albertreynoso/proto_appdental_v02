name: proto_appdental_v02
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.1.1
  firebase_database: ^12.0.2
  cloud_firestore: ^6.0.2
  intl: ^0.18.1 
  firebase_auth: ^6.1.1
  fluttertoast: ^9.0.0
  google_fonts: 5.1.0
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/img/
